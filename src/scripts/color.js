import { rN, trueOrFalse } from 'fcts.js'

    var aColor = []
    for (let i = 0; i < 5; i++) {
        // 1. Color text
        // 2. Background
        // 3. Color object
        // Button color + span color ? 

    }
export { aColor }